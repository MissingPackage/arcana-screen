# Recovery verification — 13 July 2026

This log replaces the unsupported blanket claim that Horizons 1 and 2 were complete. It records only evidence reproduced during the recovery pass.

## Automated evidence

Run from `arcana-screen/`:

```text
npm test       17 files, 50 tests passed
npm run build  TypeScript and Vite production build passed
npm run lint   ESLint passed
npm audit --omit=dev 0 production vulnerabilities
```

The suite currently covers:

- Focus continuity and Screen persistence;
- Screen create/rename/duplicate/delete/restore and Prepare/Run state;
- Narrative beat, Social clock and Exploration moment transitions;
- encounter sort, turn/round wrapping, damage and temporary HP;
- wall-clock Timer arithmetic without real waits;
- dice notation parsing, errors and deterministic execution;
- Quick Capture trimming and `/` focus behavior;
- capture review/promotion into Session Notebook and Notebook title/body/light-format persistence;
- Quick Reference URL validation, draft preservation and HTTPS normalization;
- invalid storage quarantine, recovery snapshots and backup validation/import;
- Run component hierarchies and core Focus interactions.
- capture inbox review/edit/keep/delete/promotion and Exploration active-moment invariants;
- Combat Run temporary HP, free-form conditions and source-complete quick reference.

## Browser evidence

Browser: Codex in-app browser, local Vite app at `http://127.0.0.1:5173/`.

- Entered Run from an existing persisted Screen.
- Entered Prepare, created a temporary Blank Screen, renamed it and duplicated it; verified Delete exposed Undo, then removed the temporary fixture and returned to the original Screen collection.
- Captured `Browser QA: the east ward cracked` in Narrative.
- Switched to Social and confirmed the capture remained visible.
- Advanced Scene Clock from 3/8 to 4/8.
- Switched Social → Exploration → Social and confirmed clock and capture continuity.
- Reloaded and confirmed Run mode, Social Focus, 4/8 clock and capture all persisted.
- Entered an invalid Dice formula and confirmed an actionable inline error; then rolled `2d6+3` and `d20+4` with Advantage successfully, including raw-roll breakdown.
- Started the Timer, observed wall-clock progress, reloaded and confirmed it remained running with the same absolute-time remainder; paused it after the check.
- Switched to dark theme and verified computed backgrounds changed on body, Run and notebook surfaces; enabled reduced motion and verified computed animation/transition durations were reduced to `0.01ms`, then restored the original settings.
- Opened Data & recovery, passed the local storage health check and confirmed invalid pasted JSON produced inline validation without importing anything.
- At 390×844: document `scrollWidth` equaled viewport width and no visible interactive control exceeded the horizontal viewport.
- At 768×1024: document `scrollWidth` equaled viewport width and no visible interactive control exceeded the horizontal viewport.
- At 1487×1058: captured all four Focus implementations for visual comparison.
- On an isolated clean origin (`http://[::1]:5173/`), exercised General, Combat and Blank previews, optional naming and direct General Screen creation.
- Narrative: selected outline sections, completed a beat, advanced pacing and moved from beat 1 to beat 2; reloaded and confirmed the current beat and completed state persisted.
- Social: edited the scene title, cycled an NPC attitude, confirmed destructive clock reset, restored the clock to 4/8 and promoted a capture into the notebook.
- Exploration: added and selected a moment, added a clue and chronological log entry, exercised discovery clock and counter reset/restore, completed the moment and confirmed exactly one moment remained active after reload.
- Combat: edited HP, temporary HP and free-form conditions; advanced through all seven combatants into round 4; confirmed reset and restored the entire encounter with Undo; inspected the external rules source.
- Capture review: created a capture, edited it, promoted it into Narrative, observed its promoted state and deleted it from the inbox.
- Dice: executed Disadvantage in the browser and confirmed the raw-roll breakdown; Timer duration was changed to 2:00 and restored to 60:00 from Run.
- The 390×844 pass found the header utility layer overlapping the visible Combat Focus button. The media-query grid placement was corrected, then the Focus switch, Combat live editor, capture-review panel and zero horizontal overflow were re-verified at 390×844 and 768×1024.

## Design QA evidence

Sources:

- `reference/selected-run-target.png` — Combat;
- `reference/focus-social.png` — Social;
- `reference/focus-exploration.png` — Exploration;
- `reference/focus-narrative.png` — Narrative.

Implementation screenshots and combined source/implementation comparisons are stored in `.codex/product-design/recovery-audit/` as files `03` through `10`.

Visible gaps found in the first comparison were not waived. The recovery pass added:

- Social-specific notebook content, beats, open threads and recent capture;
- Exploration Session Flow, ledger, location context, discovery clock and counter;
- Narrative outline, beats, margin notes, context drawer and pacing;
- Combat context capture, on-demand HP controls and reset/undo affordances;
- common Cinzel/Inter typography, navy/parchment palette and Phosphor iconography.
- capture inbox review and source-aligned Combat conditions, cover rules, encounter notes and external reference.

The P0–P2 design-fidelity pass is recorded in `design-qa.md` with `final result: passed`. Remaining visual differences are P3 polish, not broken hierarchy, missing core interaction or source divergence.

## Still open

- Moderated DM usability tests and time-to-task measurements.
- Timed Screen-delete Undo recovery remains unit-covered; encounter reset/Undo is browser-covered.
- Cross-browser backup/import/recovery fixture.
- Longer background suspension proof for Timer across browser lifecycle events.
- Keyboard-only, touch, 200% zoom and WCAG 2.2 AA audit.
- Development-tool dependency audit reports 11 fixable findings (2 low, 4 moderate, 5 high); the production dependency audit is clean. Dependency upgrades belong to the hardening pass and were not auto-applied during product recovery.

## M3 hardening verification — 13 July 2026

The hardening pass supersedes the development-dependency finding above: `npm audit --audit-level=high` now reports zero known vulnerabilities after compatible upgrades.

Automated gates reproduced from `arcana-screen/`:

```text
npm run test:ci
  build passed
  lint passed with jsx-a11y recommended rules
  17 Vitest files / 50 tests passed
  JS 448.5 KiB / 500 KiB
  CSS 84.0 KiB / 100 KiB

Playwright local matrix
  Chromium desktop passed
  Firefox desktop passed
  Chromium mobile 390x844 passed
  25 passed / 2 desktop touch-only skips
```

The Playwright suite covers first run, Prepare and Run axe scans; skip navigation; 200% reflow equivalent; 44 px coarse-pointer targets; non-drag structural reorder; reduced-motion persistence and computed durations; create/capture/reload; export/import round trip; startup and mode-switch budgets; and the absence of cross-origin runtime requests.

The in-app browser pass on the production build confirmed `scrollWidth === clientWidth` at 1280×720, 390×844 and 640×720, with no visible interactive control clipped horizontally and no application console error.

WebKit is configured as a blocking CI project. Its downloaded Ubuntu fallback cannot launch on this Fedora workstation because `libicudata.so.74` and related host libraries require administrator installation; `npx playwright install --with-deps webkit` in GitHub Actions supplies them. A tagged release and production deployment were intentionally not performed from the dirty local worktree.
